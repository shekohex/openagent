{
  "name": "@openagent/backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "convex dev",
    "dev:setup": "convex dev --configure --until-success",
    "codegen": "convex codegen",
    "check-types": "tsc --noEmit",
    "test": "bun run test:once",
    "test:once": "vitest run",
    "test:debug": "vitest --inspect-brk --no-file-parallelism",
    "test:coverage": "vitest run --coverage --coverage.provider=istanbul --coverage.reporter=text"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^24.3.1",
    "@vitest/coverage-v8": "3.2.4",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@convex-dev/better-auth": "^0.8.0-alpha.9",
    "@convex-dev/rate-limiter": "^0.2.12",
    "@edge-runtime/vm": "^5.0.0",
    "better-auth": "1.3.8",
    "convex": "^1.26.2",
    "convex-helpers": "^0.1.104",
    "convex-test": "^0.0.38",
    "jose": "^6.1.0",
    "vitest": "^3.2.4"
  }
}
